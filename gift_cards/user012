<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Farewell Party | SHIHSU SHIKSHA SADAN</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;800&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

<style>
:root {
    --primary: #7c6cff;
    --secondary: #ff7aa2;
    --gold: #ffd700;
    --glass-bg: rgba(255,255,255,0.12);
    --glass-border: rgba(255,255,255,0.25);
    --text: #e2e8f0;
    --text-light: #cbd5e1;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    min-height: 100vh;
    padding: 50px 20px 90px;
    background:
        radial-gradient(circle at 10% 20%, rgba(124,108,255,0.25), transparent 45%),
        radial-gradient(circle at 90% 80%, rgba(255,122,162,0.25), transparent 45%),
        linear-gradient(135deg, #0b1022, #111b3a);
    color: var(--text);
    overflow-x: hidden;
    position: relative;
}

/* ORBS */
.orb {
    position: fixed;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.55;
    z-index: -1;
    animation: float 18s infinite alternate ease-in-out;
}
.orb.one { width: 380px; height: 380px; background: #7c6cff; top: 10%; left: -120px; }
.orb.two { width: 420px; height: 420px; background: #ff7aa2; bottom: 5%; right: -150px; animation-delay: 4s; }
.orb.three { width: 260px; height: 260px; background: #ffd700; top: 70%; left: 10%; animation-delay: 7s; }

@keyframes float {
    from { transform: translate(0,0); }
    to { transform: translate(40px,-60px); }
}

/* CARD */
.invitation-card {
    max-width: 720px;
    margin: auto;
    padding: 65px 55px;
    border-radius: 32px;
    background: linear-gradient(135deg, rgba(255,255,255,0.18), rgba(255,255,255,0.06));
    backdrop-filter: blur(22px) saturate(160%);
    border: 1px solid var(--glass-border);
    box-shadow: 0 40px 80px rgba(0,0,0,0.55);
    text-align: center;
}

/* TEXT */
.event-title {
    font-family: 'Playfair Display', serif;
    font-size: 3.6rem;
    color: #fff;
}
.school-name {
    font-size: 2rem;
    color: var(--text-light);
    margin-top: 12px;
}
.divider {
    width: 110px;
    height: 3px;
    margin: 32px auto;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
}

/* WISHES */
.wishes {
    font-size: 1.45rem;
    line-height: 1.75;
    color: var(--text-light);
    margin-bottom: 40px;
}
.wish-line { display: block; margin: 14px 0; }
.wish-line::before { content: "✦"; color: var(--gold); margin-right: 14px; }

/* IMAGE */
.card-image {
    max-width: 360px;
    width: 100%;
    margin: 10px auto 38px;
    border-radius: 24px;
    box-shadow: 0 25px 55px rgba(0,0,0,0.45);
    border: 2px solid rgba(255,255,255,0.35);
}

/* DATE */
.date-box {
    padding: 34px;
    border-radius: 24px;
    background: rgba(255,255,255,0.14);
    backdrop-filter: blur(18px);
    border: 1px solid rgba(255,255,255,0.3);
    display: inline-block;
}
.date-text {
    font-family: 'Playfair Display', serif;
    font-size: 2.7rem;
    color: var(--gold);
    margin-bottom: 16px;
}
.time-venue {
    display: flex;
    flex-direction: column;
    gap: 16px;
    font-size: 1.25rem;
}

/* BUTTON */
.btn {
    margin-top: 32px;
    padding: 22px 60px;
    border-radius: 60px;
    border: none;
    font-size: 1.45rem;
    font-weight: 600;
    cursor: pointer;
    color: #fff;
    background: linear-gradient(135deg, var(--primary), #9f7cff);
}

/* QUOTE */
.quote {
    margin-top: 50px;
    font-size: 1.35rem;
    font-style: italic;
    color: #a5b4fc;
}

/* ===== DEVELOPER CREDIT (SUBTLE) ===== */
.dev-credit {
    position: fixed;
    bottom: 18px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 0.85rem;
    opacity: 0.55;
    transition: opacity 0.3s ease;
}
.dev-credit a {
    color: #c7d2fe;
    text-decoration: none;
    padding: 6px 14px;
    border-radius: 20px;
    background: rgba(255,255,255,0.08);
    backdrop-filter: blur(10px);
}
.dev-credit:hover {
    opacity: 0.9;
}

@media (max-width: 768px) {
    .invitation-card { padding: 45px 28px; }
    .event-title { font-size: 2.9rem; }
    .school-name { font-size: 1.7rem; }
}
</style>
</head>

<body>

<div class="orb one"></div>
<div class="orb two"></div>
<div class="orb three"></div>

<div class="invitation-card">

    <h1 class="event-title">Farewell Party</h1>
    <p class="school-name">Shishu Shiksha Sadan Sr. Sec. School</p>

    <div class="divider"></div>

    <div class="wishes">
        <span class="wish-line">Where every classroom held a story, and every corridor echoed with dreams.</span>
        <span class="wish-line">You weren’t just students—you were the heartbeat of this school.</span>
        <span class="wish-line">Today, we celebrate you—not goodbye, but “until next time.”</span>
    </div>

    <img
        src="https://dl.dropboxusercontent.com/scl/fi/dgxm7ygoa8wijsxtubk42/IMG_20260129_170628.jpg?rlkey=u4zmcrjfkvo8pjuvbi4u00c83&st=4cshfg4m&dl=0"
        alt="Farewell Card"
        class="card-image"
    >

    <div class="date-box">
        <div class="date-text">31st January 2026</div>
        <div class="time-venue">
            <span><i class="fas fa-clock"></i> 1:30 PM onwards</span>
            <span><i class="fas fa-map-marker-alt"></i> School Auditorium</span>
        </div>
    </div>

    <button class="btn">
        <i class="fas fa-heart"></i> Confirm Attendance
    </button>

    <p class="quote">
        The best farewells aren’t endings—they’re promises whispered in memory, waiting to bloom again.
    </p>
</div>

<!-- SUBTLE DEVELOPER BUTTON -->
<div class="dev-credit">
    <a href="https://www.squadvertex.great-site.net/" target="_blank" rel="noopener">
        Meet the Developer
    </a>
</div>

</body>
</html>